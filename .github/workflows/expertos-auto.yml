name: MercadosExpertos AutoTweet [DISABLED]

on:
  # DESACTIVADO - Migrado a bots-twitter-automated
  # schedule:
  #   - cron: '0 */6 * * *'  # Cada 6 horas
  workflow_dispatch:  # Manual only

jobs:
  tweet:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - run: pip install -r requirements.txt
    - name: Run MercadosExpertos Bot
      env:
        PPLX_API: ${{ secrets.PPLX_API }}
        TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
        TWITTER_API_KEY_SECRET: ${{ secrets.TWITTER_API_KEY_SECRET }}
        TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
        TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
        PERPLEXITY_MODEL: ${{ secrets.PERPLEXITY_MODEL || 'sonar' }}
        TOPIC: ${{ secrets.TOPIC_EXPERTOS || 'Powell Dimon Villar tasas' }}
      run: python bot_mercados_expertos.py